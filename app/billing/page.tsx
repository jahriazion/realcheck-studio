export default function Billing() {
  return (
    <main className="min-h-dvh p-6">
      <h1 className="text-2xl font-semibold mb-2">Billing</h1>
      <p className="text-white/60">Stripe integration is wired in server-side endpoints. Add your keys in .env and we can enable Checkout next.</p>
    </main>
  );
}
