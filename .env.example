# === Auth.js v5 (NextAuth v5) ===
AUTH_URL=http://localhost:3000
AUTH_SECRET=PASTE_A_LONG_RANDOM_STRING_HERE

# Fallback legacy names (not used by v5, kept for reference)
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=PASTE_A_LONG_RANDOM_STRING_HERE

# === OpenAI ===
OPENAI_API_KEY=sk-proj-...  # Project API key

# === Database ===
DATABASE_URL="file:./dev.db"

# === Public app URL ===
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === Stripe (optional; not installed in v4) ===
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PRICE_ID=price_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# === Dev overrides ===
# Set to 'true' to treat every user as Pro without Stripe (for local testing)
RC_DEV_ALL_PRO=false

# === Branding (safe to expose) ===
NEXT_PUBLIC_BRAND_NAME=RealCheck
NEXT_PUBLIC_BRAND_PRIMARY=#10b981
NEXT_PUBLIC_BRAND_ACCENT=#60a5fa
